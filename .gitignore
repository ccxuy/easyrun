# 依赖二进制 (dep/install-deps.sh 安装或手动拷贝)
dep/task
dep/yq
dep/ytt

# EZ 运行时数据
.ez/
.ez-logs/
.ez-artifacts/
.ez-state/
.ez-results/
workspace/

# 导出产物
*.tar.gz

# Server: 自定义证书和 JS 库 (构建时下载)
server/certs/*.crt
server/certs/*.pem
